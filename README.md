# Python Logging Examples

A collection of Python logging examples ranging from basic to advanced usage.

## Structure

- `basic_logger/` — Simple logging setup examples
- `advanced_logger/` — Advanced logging with custom handlers and formatters
- `logs/` — Log output directory (tracked via `.gitkeep`)
